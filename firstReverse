//function should take a string and return that string in reversed order

function FirstReverse(str) { 
  str = str.split("");
  var reverse = [];
  for (var i = str.length -1; i >=0; i--) {
     reverse.push(str[i]);
  }
  return reverse.join(""); 
}
