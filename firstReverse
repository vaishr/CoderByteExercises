function FirstReverse(str) { 

  var str = str.split("");
  var reverse = [];
  for (var i = str.length -1; i >=0; i--) {
     reverse.push(str[i]);
  }
  
  var str = reverse.join("");
  return str; 

}