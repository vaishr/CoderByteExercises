//function should return true if argument is a prime number, and false if not

function PrimeTime(num) { 
  for (var i = 2; i<num; i++) {
    if (num % i === 0) 
      return false;
   }
  return true;
}
