function ABCheck(str) {
    str = str.toLowerCase();
    var arr = str.split("");
    
    for (var i = 0; i< arr.length; i++) {
        if ((arr[i] === 'a' && arr[i+4] == 'b') ||    (arr[i] === 'b' && arr[i+4] === 'a'))
        return true;
    }
    
    return false;
}
