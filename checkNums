function should take 2 parameters (num1, num2) and return true if num 2 is greater than num1, false if num1 is greater than num2, and -1 if they are equal

function CheckNums(num1,num2) { 
  if (num1 < num2)
    return true;
  else if (num1 > num2)
    return false;
  else return -1; 
         
}
   
